spring:
    application:
        name: springboot
    datasource:
        url: jdbc:postgresql://127.0.0.1:5432/postgres
        username: postgres
        password: postgres


# MyBatis Configuration
mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.sandbox.springboot.model
